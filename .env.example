# Connection string Postgres (Railway, schema monday_public, lecture seule).
# Format attendu : postgresql://USER:PASSWORD@maglev.proxy.rlwy.net:31175/railway
# SSL est forcé côté code avec rejectUnauthorized: false (cert auto-signé Railway).
MONDAY_PUBLIC_DB_URL=
